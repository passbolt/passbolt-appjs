<div class="secret-copy">
    <a id="grid_secret_copy_{{ item.id }}">
        <span>{{ __('copy password to clipboard') }}</span>
    </a>
    <pre>{{ cellValue[0] ? cellValue[0].data : '' }}</pre>
</div>
